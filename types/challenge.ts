/**
 * Shared challenge contracts used across the product.
 * Runtime engine types live in `@/features/challenge/engine`.
 */

export type {
  ChallengeCategoryId,
  ChallengeQuestionConfig,
  ChallengeSessionState,
  ChallengeAnswerRecord,
  DimensionScores,
  QuestionType,
  SignatureDimension,
} from "@/features/challenge/engine/types";
