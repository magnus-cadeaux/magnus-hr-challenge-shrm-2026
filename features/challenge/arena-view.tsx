"use client";

import { FeatureStage } from "@/components/layout/feature-stage";

export function ArenaView() {
  return (
    <FeatureStage
      eyebrow="Lobby"
      title="Arena"
      subtitle="The gateway into the live challenge floor—ready for briefing and launch."
    />
  );
}
