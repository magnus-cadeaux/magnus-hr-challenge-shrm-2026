import type { RecentParticipant } from "./types";

/** No fabricated recent participants — live list comes from `loadLiveAdminMetrics`. */
export const MOCK_RECENT_PARTICIPANTS: RecentParticipant[] = [];
